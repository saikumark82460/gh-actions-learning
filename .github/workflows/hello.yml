name: Hello

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: ["main"]  

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Say hello
        run: echo "🎉 Hello, GitHub Actions!"
      - name: Current directory
        run: pwd
      - name: Lists
        run: ls -a    
      - name: Long List
        run: ls -al
